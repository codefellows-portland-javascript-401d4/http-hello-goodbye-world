const server = require('./server');
const router = require('./router');
const assert = require('assert');

describe('diplaying different messages', function(){
    it('displays a hello message', function(done){
      
      };
//   it('displays a godbye message', function(done){
//      function test() {
//        console.log('test 1 running');
//        console.log('here is the directory', (__dirname).slice(0,-5) + '/data/elements');
//       assert.ok(fs.existsSync((__dirname).slice(0,-5) + '/data/elements','../data/elements'));
//        done();
//       };
//   it('displays an error message when the url is unknown', function(done){

//        console.log('here is the directory', (__dirname).slice(0,-5) + '/data/elements');
//       assert.ok(fs.existsSync((__dirname).slice(0,-5) + '/data/elements','../data/elements'));
//        done();
//       };
    });


